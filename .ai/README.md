# .ai

This directory stores AI-facing project context that should travel with the GitHub repository.

- `skills/`: project-specific `SKILL.md` folders.
- `shared-skills/`: optional git submodule or checkout of reusable skills.
- `notes/`: durable project notes that Claude and Codex should both remember.
